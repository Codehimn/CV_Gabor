---
import Terminal from "./Terminal.astro";
import ParticlesBackground from "./ParticlesBackground";
import { ViewTransitions } from "astro:transitions";
---

<section class="hero zoom-section" id="hero">
    <div class="zoom-bg hero-bg-gradient"></div>
    <ParticlesBackground client:visible />

    <!-- Animated grid -->
    <div class="hero-grid parallax-layer" data-speed="-0.1"></div>

    <!-- Floating orbs -->
    <div class="orb orb-1 parallax-layer" data-speed="-0.2"></div>
    <div class="orb orb-2 parallax-layer" data-speed="-0.15"></div>
    <div class="orb orb-3 parallax-layer" data-speed="-0.25"></div>

    <div class="hero-container container">
        <div class="hero-content zoom-content">
            <div class="hero-badge reveal">
                <div class="badge-content">
                    <span class="badge-dot"></span>
                    <span class="badge-text" data-i18n="hero.badge"
                        >Available for new opportunities</span
                    >
                </div>
            </div>

            <h1
                class="hero-title reveal-cinematic"
                style="animation-delay: 0.2s"
            >
                <span class="hero-greeting" data-i18n="hero.greeting"
                    >Hi, I'm</span
                >
                <span class="hero-name gradient-text">Gabor Flandorffer</span>
            </h1>

            <div class="hero-role reveal" style="transition-delay: 0.4s">
                <span class="typing-text" id="typing-text"></span>
                <span class="cursor">|</span>
                <!-- Hidden element for i18n data sourcing -->
                <span
                    id="roles-source"
                    data-i18n="hero.roles"
                    style="display:none;"></span>
            </div>

            <p
                class="hero-description reveal"
                style="transition-delay: 0.6s"
                data-i18n="hero.description"
            >
                <strong>11+ years</strong> crafting scalable backend systems, automation
                pipelines, and full-stack solutions.
            </p>

            <div class="hero-stats reveal stagger-children">
                <div class="stat">
                    <div class="stat-value">
                        <span class="stat-number" data-count="11">0</span>
                        <span class="stat-suffix">+</span>
                    </div>
                    <span class="stat-label" data-i18n="hero.years"
                        >Years Experience</span
                    >
                </div>
                <div class="stat">
                    <div class="stat-value">
                        <span class="stat-number" data-count="50">0</span>
                        <span class="stat-suffix">+</span>
                    </div>
                    <span class="stat-label" data-i18n="hero.projects"
                        >Projects</span
                    >
                </div>
            </div>

            <div class="hero-actions reveal">
                <a
                    href="#experience"
                    class="btn btn-primary magnetic"
                    data-i18n="hero.cta_primary"
                >
                    Explore My Work
                </a>
                <a
                    href="/pdf/CV Gabor Flandorffer 2026 ENG.pdf"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="btn btn-outline magnetic hero-download-btn"
                    id="hero-download-cv"
                    data-cv-en="/pdf/CV Gabor Flandorffer 2026 ENG.pdf"
                    data-cv-es="/pdf/CV Gabor Flandorffer 2026 ENG.pdf"
                >
                    <span data-i18n="hero.download_cv">Download CV</span>
                    <span class="cv-soon-note" data-i18n="hero.download_cv_notice"
                        >Will be updated soon</span
                    >
                </a>
                <a
                    href="#contact"
                    class="btn btn-outline magnetic"
                    data-i18n="hero.cta_secondary"
                >
                    Get In Touch
                </a>
            </div>
        </div>

        <div class="hero-visual reveal">
            <Terminal />
        </div>
    </div>

    <!-- Scroll indicator -->
    <div class="scroll-indicator">
        <div class="mouse">
            <div class="wheel"></div>
        </div>
        <span data-i18n="hero.scroll">Scroll down</span>
    </div>
</section>

<script>
    function teardownHero() {
        const runtime = ((window as any).__cvRuntime ||= {});

        if (runtime.heroTypeTimeout) {
            clearTimeout(runtime.heroTypeTimeout);
            runtime.heroTypeTimeout = null;
        }

        if (runtime.heroCounterObserver) {
            runtime.heroCounterObserver.disconnect();
            runtime.heroCounterObserver = null;
        }

        if (runtime.heroCounterRafs) {
            runtime.heroCounterRafs.forEach((id: number) => cancelAnimationFrame(id));
            runtime.heroCounterRafs = [];
        }

        if (runtime.heroLangChangedHandler) {
            window.removeEventListener("langChanged", runtime.heroLangChangedHandler);
            runtime.heroLangChangedHandler = null;
        }
    }

    function initHero() {
        teardownHero();

        const runtime = ((window as any).__cvRuntime ||= {});
        const stateToken = Symbol("hero-state");
        runtime.heroStateToken = stateToken;

        function isStale() {
            return runtime.heroStateToken !== stateToken;
        }

        // Typing effect logic
        const rolesSource = document.getElementById("roles-source");
        const typingEl = document.getElementById("typing-text");

        let roles = [
            "Senior Full-Stack Developer",
            "Automation Architect",
            "Backend Specialist",
        ];
        let roleIndex = 0;
        let charIndex = 0;
        let isDeleting = false;

        function updateRoles() {
            if (rolesSource && rolesSource.textContent) {
                roles = rolesSource.textContent.split(",");
            }
        }

        function typeWriter() {
            if (isStale()) return;
            if (!typingEl) return;
            if (roleIndex >= roles.length) roleIndex = 0;
            const currentRole = roles[roleIndex];

            if (!isDeleting) {
                typingEl.textContent = currentRole.substring(0, charIndex + 1);
                charIndex++;
                if (charIndex === currentRole.length) {
                    isDeleting = true;
                    runtime.heroTypeTimeout = window.setTimeout(typeWriter, 2000);
                    return;
                }
            } else {
                typingEl.textContent = currentRole.substring(0, charIndex - 1);
                charIndex--;
                if (charIndex === 0) {
                    isDeleting = false;
                    roleIndex = (roleIndex + 1) % roles.length;
                }
            }
            runtime.heroTypeTimeout = window.setTimeout(typeWriter, isDeleting ? 40 : 80);
        }

        // Counter animation
        const counters = document.querySelectorAll(".stat-number[data-count]");
        const counterRafs: number[] = [];

        function animateCounter(targetEl: HTMLElement, target: number) {
            const duration = 1200;
            const start = performance.now();

            const frame = (now: number) => {
                if (isStale()) return;
                const progress = Math.min((now - start) / duration, 1);
                targetEl.textContent = Math.floor(progress * target).toString();

                if (progress < 1) {
                    const rafId = requestAnimationFrame(frame);
                    counterRafs.push(rafId);
                } else {
                    targetEl.textContent = target.toString();
                }
            };

            const rafId = requestAnimationFrame(frame);
            counterRafs.push(rafId);
        }

        const counterObserver = new IntersectionObserver(
            (entries) => {
                entries.forEach((entry) => {
                    if (!entry.isIntersecting || isStale()) return;

                    const targetEl = entry.target as HTMLElement;
                    const target = parseInt(targetEl.dataset.count || "0", 10);
                    animateCounter(targetEl, target);
                    counterObserver.unobserve(entry.target);
                });
            },
            { threshold: 0.5 },
        );
        runtime.heroCounterObserver = counterObserver;
        runtime.heroCounterRafs = counterRafs;

        // Initialize
        updateRoles();
        typeWriter();
        counters.forEach((c) => counterObserver.observe(c));

        // Hero CV download language switch
        const heroCvBtn = document.getElementById(
            "hero-download-cv",
        ) as HTMLAnchorElement;
        function updateHeroCv() {
            if (!heroCvBtn) return;
            const lang = localStorage.getItem("preferred-lang") || "en";
            heroCvBtn.href =
                lang === "es"
                    ? heroCvBtn.dataset.cvEs || ""
                    : heroCvBtn.dataset.cvEn || "";
        }
        updateHeroCv();

        // Language change reactive listener
        runtime.heroLangChangedHandler = () => {
            if (isStale()) return;
            setTimeout(() => {
                if (isStale()) return;
                updateRoles();
                charIndex = 0;
                isDeleting = false;
                if (runtime.heroTypeTimeout) {
                    clearTimeout(runtime.heroTypeTimeout);
                    runtime.heroTypeTimeout = null;
                }
                typeWriter();
                updateHeroCv();
            }, 50);
        };

        window.addEventListener("langChanged", runtime.heroLangChangedHandler);
    }

    const runtime = ((window as any).__cvRuntime ||= {});
    if (!runtime.heroLifecycleBound) {
        document.addEventListener("astro:after-swap", initHero);
        document.addEventListener("astro:before-swap", teardownHero);
        runtime.heroLifecycleBound = true;
    }

    initHero();
</script>

<style>
    .hero {
        min-height: clamp(620px, 86svh, 760px);
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        overflow: hidden;
        padding: 2.6rem 0 2rem;
    }

    .hero-bg-gradient {
        background:
            radial-gradient(
                ellipse 80% 60% at 50% 40%,
                rgba(56, 189, 248, 0.08) 0%,
                transparent 70%
            ),
            radial-gradient(
                ellipse 60% 40% at 30% 60%,
                rgba(129, 140, 248, 0.06) 0%,
                transparent 60%
            ),
            radial-gradient(
                ellipse 50% 50% at 70% 30%,
                rgba(244, 114, 182, 0.04) 0%,
                transparent 50%
            );
    }

    .hero-grid {
        position: absolute;
        inset: 0;
        background-image:
            linear-gradient(rgba(56, 189, 248, 0.03) 1px, transparent 1px),
            linear-gradient(
                90deg,
                rgba(56, 189, 248, 0.03) 1px,
                transparent 1px
            );
        background-size: 60px 60px;
        mask-image: radial-gradient(
            ellipse 70% 60% at 50% 50%,
            black,
            transparent
        );
        -webkit-mask-image: radial-gradient(
            ellipse 70% 60% at 50% 50%,
            black,
            transparent
        );
    }

    .hero-container {
        display: grid;
        grid-template-columns: 1.2fr 0.8fr;
        align-items: center;
        gap: 2.4rem;
        z-index: 2;
    }

    .hero-content {
        text-align: left;
    }

    .hero-badge {
        display: inline-flex;
        margin-bottom: 1.15rem;
    }

    .badge-content {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.6rem 1.4rem;
        border-radius: 100px;
        background: rgba(52, 211, 153, 0.05);
        border: 1px solid rgba(52, 211, 153, 0.2);
        color: var(--accent-emerald);
        font-size: 0.85rem;
        font-weight: 600;
        letter-spacing: 0.02em;
        box-shadow: 0 0 20px rgba(52, 211, 153, 0.05);
    }

    .badge-dot {
        width: 10px;
        height: 10px;
        background: var(--accent-emerald);
        border-radius: 50%;
        position: relative;
    }

    .badge-dot::after {
        content: "";
        position: absolute;
        inset: -4px;
        border-radius: 50%;
        border: 2px solid var(--accent-emerald);
        animation: badge-pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }

    @keyframes badge-pulse {
        0% {
            transform: scale(1);
            opacity: 0.8;
        }
        100% {
            transform: scale(2.5);
            opacity: 0;
        }
    }

    .hero-greeting {
        display: block;
        font-size: clamp(1.1rem, 2.5vw, 1.5rem);
        font-weight: 400;
        color: var(--text-secondary);
        margin-bottom: 0.5rem;
    }

    .hero-title {
        margin-bottom: 1rem;
    }

    .hero-name {
        font-size: clamp(2.8rem, 7vw, 5rem);
        font-weight: 900;
        letter-spacing: -0.02em;
        line-height: 1.1;
    }

    .hero-role {
        font-family: var(--font-mono);
        font-size: clamp(1rem, 2.5vw, 1.4rem);
        color: var(--accent-blue);
        margin-bottom: 1rem;
        min-height: 2rem;
    }

    .cursor {
        animation: blink 0.8s infinite;
        color: var(--accent-blue);
    }

    @keyframes blink {
        0%,
        100% {
            opacity: 1;
        }
        50% {
            opacity: 0;
        }
    }

    .hero-description {
        font-size: clamp(1.1rem, 2vw, 1.25rem);
        color: var(--text-secondary);
        max-width: 550px;
        margin-bottom: 1.5rem;
        line-height: 1.62;
    }

    .hero-description strong {
        color: var(--text-primary);
    }

    .hero-stats {
        display: flex;
        gap: 2.3rem;
        margin-bottom: 1.7rem;
    }

    .stat-value {
        display: flex;
        align-items: baseline;
    }

    .stat-number {
        font-size: clamp(2rem, 5vw, 3rem);
        font-weight: 800;
        background: var(--gradient-primary);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .stat-suffix {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--accent-blue);
        margin-left: 2px;
    }

    .stat-label {
        display: block;
        font-size: 0.75rem;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-top: 0.5rem;
    }

    .hero-actions {
        display: flex;
        gap: 0.9rem;
        flex-wrap: wrap;
    }

    .hero-download-btn {
        position: relative;
        align-items: center;
    }

    .cv-soon-note {
        font-size: 0.65rem;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        color: #bae6fd;
        border: 1px solid rgba(56, 189, 248, 0.28);
        background: rgba(56, 189, 248, 0.12);
        border-radius: 999px;
        padding: 0.15rem 0.45rem;
        white-space: nowrap;
    }

    .btn.magnetic {
        transition: transform 0.2s cubic-bezier(0.23, 1, 0.32, 1);
        will-change: transform;
    }

    .scroll-indicator {
        position: absolute;
        bottom: 0.6rem;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.75rem;
        color: var(--text-muted);
        font-size: 0.75rem;
        opacity: 0.6;
    }

    .mouse {
        width: 26px;
        height: 42px;
        border: 2px solid var(--text-muted);
        border-radius: 15px;
        position: relative;
    }

    .wheel {
        width: 3px;
        height: 8px;
        background: var(--accent-blue);
        border-radius: 2px;
        position: absolute;
        top: 8px;
        left: 50%;
        transform: translateX(-50%);
        animation: scroll-wheel 2s infinite;
    }

    @keyframes scroll-wheel {
        0% {
            transform: translateX(-50%) translateY(0);
            opacity: 1;
        }
        100% {
            transform: translateX(-50%) translateY(15px);
            opacity: 0;
        }
    }

    .orb {
        position: absolute;
        border-radius: 50%;
        filter: blur(100px);
        opacity: 0.4;
        animation: float 20s ease-in-out infinite;
        mix-blend-mode: screen;
    }

    .orb-1 {
        width: 400px;
        height: 400px;
        background: var(--accent-blue);
        top: -10%;
        left: -5%;
    }
    .orb-2 {
        width: 300px;
        height: 300px;
        background: var(--accent-indigo);
        bottom: 10%;
        right: -5%;
        animation-delay: -7s;
    }
    .orb-3 {
        width: 200px;
        height: 200px;
        background: var(--accent-rose);
        top: 40%;
        right: 20%;
        animation-delay: -14s;
    }

    @keyframes float {
        0%,
        100% {
            transform: translate(0, 0) scale(1);
        }
        33% {
            transform: translate(30px, -50px) scale(1.1);
        }
        66% {
            transform: translate(-20px, 20px) scale(0.9);
        }
    }

    @media (max-width: 1024px) {
        .hero-container {
            grid-template-columns: 1fr;
            text-align: center;
            gap: 2rem;
        }

        .hero-content {
            text-align: center;
        }

        .hero-description,
        .hero-stats,
        .hero-actions {
            justify-content: center;
            margin-right: auto;
            margin-left: auto;
        }
    }

    @media (max-width: 768px) {
        .hero {
            min-height: auto;
            padding-top: 6.2rem;
            padding-bottom: 1.2rem;
        }

        .hero-stats {
            gap: 2rem;
        }
        .scroll-indicator {
            display: none;
        }
    }

    @media (max-width: 430px) {
        .hero {
            padding-top: 5.5rem;
            padding-bottom: 0.9rem;
        }

        .hero-description {
            font-size: 0.97rem;
            margin-bottom: 1.1rem;
        }

        .hero-stats {
            gap: 1.4rem;
            margin-bottom: 1.15rem;
        }

        .stat-label {
            font-size: 0.68rem;
            margin-top: 0.25rem;
        }

        .hero-actions {
            gap: 0.65rem;
        }

        .cv-soon-note {
            font-size: 0.56rem;
            padding: 0.14rem 0.36rem;
        }
    }
</style>
