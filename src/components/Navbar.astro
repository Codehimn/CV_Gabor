---
const navItems = [
    { href: "#about", label: "About" },
    { href: "#experience", label: "Experience" },
    { href: "#skills", label: "Skills" },
    { href: "#projects", label: "Projects" },
    { href: "#education", label: "Education" },
    { href: "#contact", label: "Contact" },
];
---

<nav class="navbar">
    <div class="nav-container">
        <a href="/about" class="nav-logo">
            <span class="logo-bracket">&lt;</span>
            <span class="logo-name">GF</span>
            <span class="logo-bracket">/&gt;</span>
        </a>

        <ul class="nav-links" id="nav-menu">
            <li>
                <a href="/about" class="nav-link" data-i18n="nav.about">About</a
                >
            </li>
            <li>
                <a
                    href="/experience"
                    class="nav-link"
                    data-i18n="nav.experience">Experience</a
                >
            </li>
            <li>
                <a
                    href="/credentials"
                    class="nav-link"
                    data-i18n="nav.credentials">Credentials</a
                >
            </li>
            <li>
                <a href="/projects" class="nav-link" data-i18n="nav.projects"
                    >Projects</a
                >
            </li>
            <li>
                <a href="/ai" class="nav-link nav-link-ai" data-i18n="nav.ai"
                    >Ask AI ✨</a
                >
            </li>
            <li>
                <a href="/contact" class="nav-link" data-i18n="nav.contact"
                    >Contact</a
                >
            </li>
            <li>
                <a
                    href="https://wa.me/491789752993?text=Hi%20Gabor!%20I%20saw%20your%20portfolio%20and%20I'd%20like%20to%20chat."
                    target="_blank"
                    rel="noopener noreferrer"
                    class="btn btn-primary nav-cta"
                    data-i18n="nav.cta"
                >
                    Let's Talk
                </a>
            </li>
        </ul>

        <div class="nav-controls">
            <div class="lang-toggle">
                <button class="lang-btn active" data-lang="en" data-i18n-aria="nav.switch_en" aria-pressed="true">EN</button>
                <div class="lang-separator"></div>
                <button class="lang-btn" data-lang="es" data-i18n-aria="nav.switch_es" aria-pressed="false">ES</button>
            </div>
            <!-- Hamburger -->
            <button
                class="hamburger-wrapper"
                data-i18n-aria="nav.toggle_menu"
                aria-label="Toggle menu"
                aria-expanded="false"
                aria-controls="nav-menu"
            >
                <div class="hamburger">
                    <span></span>
                    <span></span>
                    <span></span>
                </div>
                <div class="hamburger-glow"></div>
            </button>
        </div>
    </div>
</nav>

<style>
    .navbar {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        z-index: 100;
        padding: 1.2rem 0;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .navbar.scrolled {
        background: rgba(10, 14, 26, 0.85);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border-bottom: 1px solid rgba(56, 189, 248, 0.08);
        padding: 0.8rem 0;
    }

    .nav-container {
        width: min(1200px, 90vw);
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .nav-logo {
        font-family: var(--font-mono);
        font-size: 1.4rem;
        font-weight: 700;
        text-decoration: none;
        color: var(--text-primary);
        transition: all 0.3s;
        z-index: 120; /* Keep logo above mobile menu */
    }

    .logo-bracket {
        color: var(--accent-blue);
    }

    .logo-name {
        background: var(--gradient-primary);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .nav-logo:hover {
        transform: scale(1.05);
    }

    .nav-links {
        display: flex;
        align-items: center;
        list-style: none;
        gap: 0.5rem;
    }

    .nav-link {
        padding: 0.5rem 0.9rem;
        color: var(--text-secondary);
        font-size: 0.9rem;
        font-weight: 500;
        text-decoration: none;
        border-radius: var(--radius-sm);
        transition: all 0.3s;
        position: relative;
    }

    .nav-link::after {
        content: "";
        position: absolute;
        bottom: 0.2rem;
        left: 50%;
        transform: translateX(-50%) scaleX(0);
        width: 60%;
        height: 2px;
        background: var(--gradient-primary);
        border-radius: 2px;
        transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .nav-link:hover {
        color: var(--text-primary);
    }

    .nav-link:hover::after,
    .nav-link.active::after {
        transform: translateX(-50%) scaleX(1);
    }

    .nav-link.active {
        color: var(--text-primary);
    }

    .nav-link-ai {
        background: linear-gradient(135deg, var(--accent-blue) 0%, var(--accent-cyan) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-weight: 700;
    }

    .nav-cta {
        padding: 0.6rem 1.4rem;
        font-size: 0.9rem;
        margin-left: 1rem;
    }

    /* Controls: Lang & Hamburger */
    .nav-controls {
        display: flex;
        align-items: center;
        gap: 1.5rem;
        z-index: 120; /* Keep controls above mobile menu */
    }

    .lang-toggle {
        display: flex;
        align-items: center;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 20px;
        padding: 4px;
    }

    .lang-separator {
        width: 1px;
        height: 12px;
        background: rgba(255, 255, 255, 0.1);
        margin: 0 2px;
    }

    .lang-btn {
        background: none;
        border: none;
        color: var(--text-muted);
        font-size: 0.75rem;
        font-weight: 600;
        padding: 4px 10px;
        border-radius: 12px;
        cursor: pointer;
        transition: all 0.3s;
    }

    .lang-btn:hover {
        color: var(--text-primary);
    }

    .lang-btn.active {
        background: var(--accent-blue);
        color: white;
        box-shadow: 0 0 10px rgba(56, 189, 248, 0.3);
    }

    /* --- Hamburger Redesign --- */
    .hamburger-wrapper {
        display: none;
        position: relative;
        width: 44px;
        height: 44px;
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: var(--radius-md);
        padding: 0;
        cursor: pointer;
        z-index: 1001;
        transition: all 0.3s var(--ease-out-expo);
        align-items: center;
        justify-content: center;
    }

    .hamburger {
        position: relative;
        width: 22px;
        height: 16px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        z-index: 2;
    }

    .hamburger span {
        width: 100%;
        height: 2px;
        background: var(--text-primary);
        border-radius: 4px;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        transform-origin: left center;
    }

    .hamburger-wrapper.active {
        background: rgba(56, 189, 248, 0.1);
        border-color: rgba(56, 189, 248, 0.3);
        box-shadow: 0 0 20px rgba(56, 189, 248, 0.2);
    }

    .hamburger-wrapper.active .hamburger span:nth-child(1) {
        transform: rotate(45deg) translate(2px, -1px);
        background: var(--accent-blue);
    }

    .hamburger-wrapper.active .hamburger span:nth-child(2) {
        opacity: 0;
        transform: scaleX(0);
    }

    .hamburger-wrapper.active .hamburger span:nth-child(3) {
        transform: rotate(-45deg) translate(2px, 1px);
        background: var(--accent-blue);
    }

    .hamburger-glow {
        position: absolute;
        inset: 0;
        border-radius: var(--radius-md);
        background: radial-gradient(
            circle at center,
            var(--accent-blue),
            transparent
        );
        opacity: 0;
        filter: blur(10px);
        transition: opacity 0.3s;
    }

    .hamburger-wrapper:hover .hamburger-glow {
        opacity: 0.15;
    }

    @media (max-width: 768px) {
        .hamburger-wrapper {
            display: flex;
        }

        .nav-links {
            position: fixed;
            top: 0;
            right: -100%;
            width: 100%;
            height: 100vh;
            background: rgba(10, 14, 26, 0.98);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 2rem;
            padding: 2rem;
            transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
            z-index: 1000;
            visibility: hidden;
            pointer-events: none;
        }

        .nav-links.open {
            right: 0;
            visibility: visible;
            pointer-events: all;
        }

        .nav-links li {
            width: 100%;
            display: flex;
            justify-content: center;
        }

        .nav-links a {
            font-size: 1.5rem;
            font-weight: 600;
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.5s var(--ease-out-expo);
        }

        .nav-links.open a {
            opacity: 1;
            transform: translateY(0);
        }

        /* Staggered entrance for links */
        .nav-links.open li:nth-child(1) a {
            transition-delay: 0.1s;
        }
        .nav-links.open li:nth-child(2) a {
            transition-delay: 0.15s;
        }
        .nav-links.open li:nth-child(3) a {
            transition-delay: 0.2s;
        }
        .nav-links.open li:nth-child(4) a {
            transition-delay: 0.25s;
        }
        .nav-links.open li:nth-child(5) a {
            transition-delay: 0.3s;
        }
        .nav-links.open li:nth-child(6) a {
            transition-delay: 0.35s;
        }
        .nav-links.open li:nth-child(7) a {
            transition-delay: 0.4s;
        }

        .nav-links .btn-primary {
            width: 100%;
            max-width: 280px;
            justify-content: center;
            margin-top: 1rem;
        }

        .nav-cta {
            margin-left: 0;
            margin-top: 1rem;
            font-size: 1.1rem;
            padding: 1rem 2rem;
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.4s ease 0.6s;
        }

        .nav-links.open .nav-cta {
            opacity: 1;
            transform: translateY(0);
        }

        .lang-toggle {
            /* Keep toggle visible on mobile */
            margin-right: 0.5rem;
        }
    }
</style>

<script>
    function initNavbar() {
        const runtime = ((window as any).__cvRuntime ||= {});
        if (runtime.navbarInitialized) {
            return;
        }

        const hamburger = document.querySelector(
            ".hamburger-wrapper",
        ) as HTMLElement | null;
        const navLinks = document.querySelector(
            ".nav-links",
        ) as HTMLElement | null;
        const layoutBody = document.body;

        if (hamburger && navLinks) {
            hamburger.addEventListener("click", () => {
                const isOpen = hamburger.classList.toggle("active");
                navLinks.classList.toggle("open", isOpen);
                layoutBody.classList.toggle("menu-open", isOpen);
                hamburger.setAttribute("aria-expanded", isOpen.toString());
            });

            // Close menu on link click
            navLinks.querySelectorAll("a").forEach((link) => {
                link.addEventListener("click", () => {
                    hamburger.classList.remove("active");
                    navLinks.classList.remove("open");
                    layoutBody.classList.remove("menu-open");
                    hamburger.setAttribute("aria-expanded", "false");
                });
            });

            // Close on escape
            document.addEventListener("keydown", (e) => {
                if (e.key === "Escape" && navLinks.classList.contains("open")) {
                    hamburger.classList.remove("active");
                    navLinks.classList.remove("open");
                    layoutBody.classList.remove("menu-open");
                    hamburger.setAttribute("aria-expanded", "false");
                }
            });

            runtime.navbarInitialized = true;
        }
    }

    function updateActiveNav() {
        const navLinksElement = document.querySelector(
            ".nav-links",
        ) as HTMLElement | null;

        if (!navLinksElement) {
            return;
        }

        const currentPath = window.location.pathname;
        navLinksElement.querySelectorAll(".nav-link").forEach((link) => {
            link.classList.remove("active");
            link.removeAttribute("aria-current");
            const href = link.getAttribute("href");
            if (
                href === currentPath ||
                (href !== "/" && currentPath.startsWith(href || ""))
            ) {
                link.classList.add("active");
                link.setAttribute("aria-current", "page");
            }
        });
    }

    initNavbar();
    updateActiveNav();

    const runtime = ((window as any).__cvRuntime ||= {});
    if (!runtime.navActivePathBound) {
        document.addEventListener("astro:after-swap", updateActiveNav);
        runtime.navActivePathBound = true;
    }
</script>
