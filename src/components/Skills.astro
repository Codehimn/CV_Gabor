---
const skillCategories = [
    {
        title: "Development Core",
        icon: "💻",
        skills: [
            { name: "Python (Expert)", level: 100, years: "9+" },
            { name: "Asyncio / Aiohttp", level: 95, years: "5+" },
            { name: "JavaScript / TS", level: 85, years: "6+" },
            { name: "C# .NET / VB.NET", level: 80, years: "7+" },
            { name: "Cython / Micropython", level: 75, years: "3+" },
            { name: "C++ / Assembly", level: 60, years: "2+" },
        ],
    },
    {
        title: "Automation & Testing",
        icon: "🤖",
        skills: [
            { name: "Puppeteer / Pyppeteer", level: 95, years: "5+" },
            { name: "AutoIt / Selenium", level: 90, years: "7+" },
            { name: "pytest / Mocking", level: 90, years: "5+" },
            { name: "OpenCV / ImageMagick", level: 80, years: "4+" },
            { name: "Web Scraping / Crawling", level: 95, years: "8+" },
            { name: "OllyDbg / Wireshark", level: 70, years: "4+" },
        ],
    },
    {
        title: "Web & Architecture",
        icon: "🏗️",
        skills: [
            { name: "Django / FastAPI", level: 95, years: "8+" },
            { name: "REST / GraphQL", level: 90, years: "6+" },
            { name: "WebSockets", level: 85, years: "5+" },
            { name: "Nginx / Apache", level: 80, years: "7+" },
            { name: "Messaging (RabbitMQ)", level: 75, years: "3+" },
            { name: "Microservices", level: 85, years: "4+" },
        ],
    },
    {
        title: "Databases",
        icon: "🗄️",
        skills: [
            { name: "PostgreSQL / MySQL", level: 90, years: "8+" },
            { name: "Oracle (AM/CW/FM)", level: 75, years: "3+" },
            { name: "SQL Server", level: 85, years: "6+" },
            { name: "MongoDB / Redis", level: 80, years: "4+" },
            { name: "SQLAlchemy / ORMs", level: 90, years: "7+" },
        ],
    },
    {
        title: "DevOps & Systems",
        icon: "☁️",
        skills: [
            { name: "Docker / Portainer", level: 90, years: "6+" },
            { name: "GitHub Actions / CI/CD", level: 90, years: "4+" },
            { name: "AWS / GCP / Cloud", level: 80, years: "4+" },
            { name: "Linux / Bash Scripting", level: 90, years: "8+" },
            { name: "Puppet / Ansible", level: 70, years: "3+" },
            { name: "Windows Server", level: 80, years: "9+" },
        ],
    },
];
---

<section class="skills zoom-section" id="skills">
    <div class="zoom-bg skills-bg"></div>
    <div class="container zoom-content">
        <div class="skills-header reveal">
            <p class="section-label" data-i18n="skills.label">
                Technical Arsenal
            </p>
            <h2 class="section-title" data-i18n="skills.title">
                The Full <span class="gradient-text">Stack & Beyond</span>
            </h2>
            <p class="section-subtitle" data-i18n="skills.subtitle">
                A deep, multi-disciplinary toolkit ranging from low-level
                automation to high-level system architecture.
            </p>
        </div>

        <div class="skills-grid stagger-children">
            {
                skillCategories.map((category) => (
                    <div class="skill-category glass-card">
                        <div class="category-header">
                            <span class="category-icon">{category.icon}</span>
                            <h3 class="category-title">{category.title}</h3>
                        </div>

                        <div class="skill-list">
                            {category.skills.map((skill) => (
                                <div class="skill-item">
                                    <div class="skill-info">
                                        <span class="skill-name">
                                            {skill.name}
                                        </span>
                                        <span class="skill-years">
                                            {skill.years} yrs
                                        </span>
                                    </div>
                                    <div class="skill-bar">
                                        <div
                                            class="skill-progress"
                                            style={`--progress: ${skill.level}%`}
                                        >
                                            <div class="skill-glow" />
                                        </div>
                                    </div>
                                </div>
                            ))}
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<script>
    const skillObserver = new IntersectionObserver(
        (entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    const bars =
                        entry.target.querySelectorAll(".skill-progress");
                    bars.forEach((bar, i) => {
                        const htmlBar = bar as HTMLElement;
                        setTimeout(() => {
                            htmlBar.style.width =
                                htmlBar.style.getPropertyValue("--progress");
                        }, i * 80);
                    });
                    skillObserver.unobserve(entry.target);
                }
            });
        },
        { threshold: 0.1 },
    );

    document.querySelectorAll(".skill-category").forEach((cat) => {
        skillObserver.observe(cat);
    });
</script>

<style>
    .skills {
        padding: var(--section-padding) 0;
        position: relative;
        overflow: hidden;
    }

    .skills::before {
        content: "";
        position: absolute;
        inset: -20%;
        background:
            radial-gradient(
                circle at 70% 30%,
                rgba(56, 189, 248, 0.05) 0%,
                transparent 50%
            ),
            radial-gradient(
                circle at 30% 70%,
                rgba(129, 140, 248, 0.05) 0%,
                transparent 50%
            );
        pointer-events: none;
    }

    .skills-header {
        text-align: center;
        margin-bottom: 4rem;
    }

    .skills-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 1.5rem;
    }

    .skill-category {
        padding: 2rem;
        transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .skill-category:hover {
        transform: translateY(-8px) scale(1.02);
        box-shadow:
            0 20px 40px rgba(0, 0, 0, 0.4),
            0 0 20px rgba(56, 189, 248, 0.1);
    }

    .category-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 2rem;
        padding-bottom: 1.25rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }

    .category-icon {
        font-size: 1.8rem;
        filter: drop-shadow(0 0 8px rgba(56, 189, 248, 0.3));
    }

    .category-title {
        font-size: 1.2rem;
        font-weight: 800;
        letter-spacing: -0.01em;
    }

    .skill-list {
        display: flex;
        flex-direction: column;
        gap: 1.25rem;
    }

    .skill-item {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .skill-info {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .skill-name {
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--text-primary);
    }

    .skill-years {
        font-family: var(--font-mono);
        font-size: 0.7rem;
        color: var(--text-muted);
        letter-spacing: 0.05em;
    }

    .skill-bar {
        height: 6px;
        background: rgba(255, 255, 255, 0.03);
        border-radius: 10px;
        position: relative;
        overflow: visible;
    }

    .skill-progress {
        height: 100%;
        width: 0;
        background: var(--gradient-primary);
        border-radius: 10px;
        transition: width 1.5s cubic-bezier(0.16, 1, 0.3, 1);
        position: relative;
    }

    .skill-glow {
        position: absolute;
        right: -4px;
        top: 50%;
        transform: translateY(-50%);
        width: 12px;
        height: 12px;
        background: var(--accent-blue);
        border-radius: 50%;
        box-shadow: 0 0 15px var(--accent-blue);
        opacity: 0.8;
    }

    @media (max-width: 768px) {
        .skills-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
