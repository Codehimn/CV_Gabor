---
const skillCategories = [
    {
        title: "Development Core",
        icon: "💻",
        span: "row-2",
        skills: [
            { name: "Python (Expert)", level: 100, years: "11+" },
            { name: "Asyncio / Aiohttp", level: 95, years: "7+" },
            { name: "JavaScript / TS", level: 85, years: "8+" },
            { name: "C# .NET / VB.NET", level: 80, years: "9+" },
            { name: "PHP", level: 70, years: "5+" },
            { name: "Cython / MicroPython", level: 75, years: "5+" },
        ],
    },
    {
        title: "Automation & Testing",
        icon: "🤖",
        span: "col-2",
        skills: [
            { name: "Puppeteer / Pyppeteer", level: 95, years: "7+" },
            { name: "AutoIt / Selenium", level: 90, years: "11+" },
            { name: "pytest / Mocking", level: 90, years: "7+" },
            { name: "Web Scraping / Crawling", level: 95, years: "11+" },
            { name: "OpenCV / ImageMagick", level: 80, years: "6+" },
        ],
    },
    {
        title: "Web & Architecture",
        icon: "🏗️",
        skills: [
            { name: "Django / FastAPI", level: 95, years: "10+" },
            { name: "REST / GraphQL / Swagger", level: 90, years: "8+" },
            { name: "Angular / Vue", level: 65, years: "3+" },
            { name: "HTML5 / CSS3 / jQuery", level: 85, years: "8+" },
            { name: "WebSockets / Microservices", level: 85, years: "6+" },
        ],
    },
    {
        title: "Databases",
        icon: "🗄️",
        skills: [
            { name: "PostgreSQL / MySQL", level: 90, years: "11+" },
            { name: "MongoDB / Redis", level: 80, years: "6+" },
            { name: "SQLAlchemy / ORMs", level: 90, years: "9+" },
            { name: "Oracle (AM, CW, FM)", level: 65, years: "3+" },
        ],
    },
    {
        title: "DevOps & Cloud",
        icon: "☁️",
        span: "col-2",
        skills: [
            { name: "Docker / Portainer", level: 90, years: "8+" },
            { name: "GitHub Actions / Jenkins", level: 90, years: "6+" },
            { name: "AWS / GCP", level: 75, years: "4+" },
            { name: "Nginx / RabbitMQ", level: 80, years: "5+" },
            { name: "Linux / Bash / Windows Server", level: 90, years: "11+" },
        ],
    },
    {
        title: "Additional Languages",
        icon: "💎",
        skills: [
            { name: "C++", level: 70, years: "4+" },
            { name: "Ruby", level: 65, years: "3+" },
            { name: "VBA", level: 80, years: "8+" },
        ],
    },
    {
        title: "Networking & Security",
        icon: "🔒",
        skills: [
            { name: "Wireshark", level: 70, years: "4+" },
            { name: "Puppet / Config Mgmt", level: 70, years: "3+" },
            { name: "Kali Linux / OllyDbg", level: 60, years: "3+" },
        ],
    },
];
---

<section class="skills" id="skills">
    <div class="container">
        <div class="skills-header reveal">
            <p class="section-label" data-i18n="skills.label">
                Technical Arsenal
            </p>
            <h2 class="section-title" data-i18n="skills.title">
                The Full <span class="gradient-text">Stack & Beyond</span>
            </h2>
            <p class="section-subtitle" data-i18n="skills.subtitle">
                A deep, multi-disciplinary toolkit ranging from low-level
                automation to high-level system architecture.
            </p>
        </div>

        <div class="bento-grid stagger-children">
            {
                skillCategories.map((category, i) => (
                    <div
                        class={`bento-item glass-card interactive ${category.span || ""}`}
                        style={`transition-delay: ${i * 0.1}s`}
                    >
                        <div class="category-header">
                            <span class="category-icon">{category.icon}</span>
                            <h3 class="category-title">{category.title}</h3>
                        </div>

                        <div class="skill-list">
                            {category.skills.map((skill) => (
                                <div class="skill-item">
                                    <div class="skill-info">
                                        <span class="skill-name">
                                            {skill.name}
                                        </span>
                                        <span class="skill-years">
                                            {skill.years} yrs
                                        </span>
                                    </div>
                                    <div class="skill-bar">
                                        <div
                                            class="skill-progress"
                                            data-progress={skill.level}
                                            style={`--progress: ${skill.level}%`}
                                        >
                                            <div class="skill-glow" />
                                        </div>
                                    </div>
                                </div>
                            ))}
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style>
    .skills {
        padding: var(--section-padding) 0;
        position: relative;
    }

    .skills-header {
        text-align: center;
        margin-bottom: 4rem;
    }

    .bento-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        grid-auto-rows: minmax(280px, auto);
        gap: 1.5rem;
    }

    .bento-item {
        padding: 2rem;
        display: flex;
        flex-direction: column;
        transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .bento-item.row-2 {
        grid-row: span 2;
    }

    .bento-item.col-2 {
        grid-column: span 2;
    }

    .bento-item:hover {
        transform: translateY(-8px) scale(1.02);
        box-shadow:
            0 20px 40px rgba(0, 0, 0, 0.4),
            0 0 20px rgba(56, 189, 248, 0.1);
    }

    .category-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 2rem;
        padding-bottom: 1.25rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }

    .category-icon {
        font-size: 1.8rem;
        filter: drop-shadow(0 0 8px rgba(56, 189, 248, 0.3));
    }

    .category-title {
        font-size: 1.2rem;
        font-weight: 800;
        letter-spacing: -0.01em;
    }

    .skill-list {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
        flex: 1;
        justify-content: center;
    }

    .skill-item {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .skill-info {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .skill-name {
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--text-primary);
    }

    .skill-years {
        font-family: var(--font-mono);
        font-size: 0.7rem;
        color: var(--text-muted);
        letter-spacing: 0.05em;
    }

    .skill-bar {
        height: 4px;
        background: rgba(255, 255, 255, 0.03);
        border-radius: 10px;
        position: relative;
    }

    .skill-progress {
        height: 100%;
        width: 0;
        background: var(--gradient-primary);
        border-radius: 10px;
        transition: width 1.5s cubic-bezier(0.16, 1, 0.3, 1);
        position: relative;
    }

    .skill-glow {
        position: absolute;
        right: -4px;
        top: 50%;
        transform: translateY(-50%);
        width: 10px;
        height: 10px;
        background: var(--accent-blue);
        border-radius: 50%;
        box-shadow: 0 0 15px var(--accent-blue);
        opacity: 0.8;
    }

    @media (max-width: 1024px) {
        .bento-grid {
            grid-template-columns: repeat(2, 1fr);
        }
        .bento-item.col-2 {
            grid-column: span 1;
        }
    }

    @media (max-width: 768px) {
        .bento-item {
            padding: 1.25rem;
        }

        .bento-grid {
            grid-template-columns: 1fr;
            grid-auto-rows: auto;
        }
        .bento-item.row-2 {
            grid-row: span 1;
        }
    }
</style>

<script>
    function teardownSkillsAnimation() {
        const runtime = ((window as any).__cvRuntime ||= {});
        if (runtime.skillsObserver) {
            runtime.skillsObserver.disconnect();
            runtime.skillsObserver = null;
        }
    }

    function initSkillsAnimation() {
        teardownSkillsAnimation();

        const runtime = ((window as any).__cvRuntime ||= {});
        const observer = new IntersectionObserver(
            (entries) => {
                entries.forEach((entry) => {
                    if (!entry.isIntersecting) return;

                    const bars = entry.target.querySelectorAll(".skill-progress");
                    bars.forEach((bar, i) => {
                        const htmlBar = bar as HTMLElement;
                        setTimeout(() => {
                            htmlBar.style.width = `${htmlBar.getAttribute("data-progress") || 0}%`;
                        }, i * 100);
                    });

                    observer.unobserve(entry.target);
                });
            },
            { threshold: 0.2 },
        );

        runtime.skillsObserver = observer;

        document.querySelectorAll(".bento-item").forEach((item) => {
            observer.observe(item);
        });
    }

    initSkillsAnimation();
    document.addEventListener("astro:after-swap", initSkillsAnimation);
    document.addEventListener("astro:before-swap", teardownSkillsAnimation);
</script>
